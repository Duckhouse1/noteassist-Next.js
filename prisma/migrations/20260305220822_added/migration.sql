BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Note] ADD [Transcript] NVARCHAR(max) NOT NULL CONSTRAINT [Note_Transcript_df] DEFAULT ' ';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
